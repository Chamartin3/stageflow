$schema: http://json-schema.org/draft-07/schema#
description: 'Schema for stage: active'
properties:
  account_status: {type: string}
  address:
    properties:
      city: {type: string}
      country: {type: string}
    required: [city, country]
    type: object
  created_at: {type: string}
  date_of_birth: {type: string}
  email: {type: string}
  email_verified_at: {type: string}
  first_name: {type: string}
  last_name: {type: string}
  password: {type: string}
  phone_number: {type: string}
  verification_token: {type: string}
required: [address, date_of_birth, email, email_verified_at, first_name, 
      last_name, password, phone_number, verification_token]
title: simple_user_profile - active (Cumulative)
type: object
